<Window x:Class="QLCAFE_WPF.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:i = "http://schemas.microsoft.com/expression/2010/interactivity"
         xmlns:local="clr-namespace:QLCAFE_WPF.Viewmodel"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Title="MainWindow" Height="500" Width="500">
    <Window.DataContext>
        <local:MainViewModel x:Name="ds"></local:MainViewModel>
    </Window.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding close}" 
                                   CommandParameter="{Binding ElementName=FloatingPasswordBox}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="_Save"/>
            </MenuItem>
            <MenuItem Header="File">
                <MenuItem Header="_Save"/>
            </MenuItem>
            <MenuItem Header="File">
                <MenuItem Header="_Save"/>
            </MenuItem>
        </Menu>
        <Grid>
            <TabControl>
                <TabItem Header="Bàn">
                    <Grid Background="#FFE5E5E5">
                        <TextBlock Text="fdfdf"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Admin">
                    <Grid Background="#FFE5E5E5">
                        <TextBlock><Run Text="123"/><InlineUIContainer>
                        		<ComboBox Width="120" ItemsSource="{Binding Path=Obs}" SelectedValue="{Binding Ac}">
                                     <ComboBox.ItemTemplate>  
                <DataTemplate>  
                    <StackPanel Orientation="Horizontal">  
                        <TextBlock Text="{Binding Path=id}"/>  
                        <TextBlock Text=" - "/>  
                        <TextBlock Text="{Binding Path=UserName}"/>  
                    </StackPanel>  
                </DataTemplate>  
            </ComboBox.ItemTemplate> 
                                </ComboBox>
                        	</InlineUIContainer></TextBlock>
                    </Grid>
                   
                </TabItem>
            </TabControl>

        </Grid>
        </DockPanel>
    
</Window>
